use dioxus::prelude::*;

// pub fn ContextProvider(cx: Scope) -> Element {
//     cx.use_hook(|_| {
//         cx.provide_context(Title("Hello".to_string()));
//     });

//     rsx!(cx, Child {})
// }
